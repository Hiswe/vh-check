extends _layout.pug

block vars
  - var pageTitle = `global`

block nav
  +menu(0)

block content
  script( src="https://unpkg.com/vh-check/dist/vh-check.min.js")
  script.
    (function () {
      var test = vhCheck();
      var message = (!test.isNeeded ? 'not ' : '') + 'needed.<br/>Gap is: ';
      message = message + test.value + 'px';
      const domCheckWrapper = document.querySelector('.js-check');
      domCheckWrapper.innerHTML = message;
    }());
